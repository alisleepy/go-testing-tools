# 测试工具集管理面板 - 需求文档

## 1. 项目概述

### 1.1 项目背景
作为一名APP测试工程师，日常工作中需要使用大量测试工具，这些工具的访问地址散落在各处（书签、文档、聊天记录等），查找不便。为解决此问题，开发一个集中化的测试工具集管理面板，将所有工具地址统一管理。

### 1.2 项目目标
- 提供统一的测试工具入口管理界面
- 支持工具信息的增删改查及排序
- 实现简单的权限控制（仅管理员可管理）

---

## 2. 技术架构

| 层级 | 技术选型 | 说明 |
|------|----------|------|
| 后端语言 | Go | 使用标准库 + 轻量级框架 |
| 前端 | HTML + CSS + JavaScript | 单页面应用 |
| 数据库 | SQLite | 嵌入式数据库，无需额外部署 |
| 部署 | 单一二进制文件 | 通过 `embed` 将 HTML 静态资源打包进二进制 |

---

## 3. 功能需求

### 3.1 全局布局

整个应用为**单页面应用**，页面顶部为导航Tab栏，包含两个Tab：

| Tab名称 | 访问权限 | 说明 |
|----------|----------|------|
| 工具集列表 | 所有人可见（无需登录） | 工具列表展示页，只读 |
| 工具管理 | 仅登录用户可见 | 工具增删改查及排序 |

**说明：**
- 未登录时访问「工具管理」Tab，显示登录弹窗/表单，或重定向至登录页（建议采用弹窗登录方式，保持单页体验）
- 登录成功后，显示「工具管理」Tab内容

---

### 3.2 登录功能

| 项目 | 说明 |
|------|------|
| 是否支持注册 | **不支持**，仅内置管理员账户 |
| 管理员账号 | `alisleepy` |
| 管理员密码 | `xiaoyouzi!@#` |
| 登录方式 | 弹窗登录或页面内登录表单（建议页面内登录，更清晰） |
| 会话保持 | 支持会话保持（Cookie/Session/Token），刷新页面后仍保持登录状态，直至主动退出或会话过期 |
| 退出登录 | 在管理页面提供退出按钮 |

---

### 3.3 工具集列表（只读）

**展示形式：** 表格

**表格字段：**

| 列名 | 对应字段 | 说明 |
|------|----------|------|
| 序号 | - | 自增序号，从1开始 |
| 工具名称 | Name | 必填，文本 |
| 工具作用 | Purpose | 必填，文本 |
| 工具地址 | URL | 必填，显示为可点击的超链接 |
| 备注信息 | Remark | 可选，文本 |

**权限控制：**
- 所有人可见，**无需登录**
- 只读展示，**不支持**编辑、删除、拖动排序

**排序规则：** 默认按照数据库中的排序字段（`sort_order`）升序排列

---

### 3.4 工具管理（仅管理员）

**展示形式：** 表格

**表格字段：**

| 列名 | 对应字段 | 说明 |
|------|----------|------|
| ID | ID | 数据库自增主键，展示为数字 |
| 工具名称 | Name | 必填，文本 |
| 工具作用 | Purpose | 必填，文本 |
| 工具地址 | URL | 必填，文本 |
| 备注信息 | Remark | 可选，文本 |
| 操作 | - | 包含「编辑」「删除」按钮 |

**排序规则：** 默认按照数据库中的排序字段（`sort_order`）升序排列

#### 3.4.1 新增工具
- 在「工具管理」表格上方提供「新增」按钮
- 点击后弹出新增弹窗，包含以下字段：
  - 工具名称（文本输入，必填）
  - 工具作用（文本输入，必填）
  - 工具地址（文本输入，必填，需校验URL格式）
  - 备注信息（文本输入，选填）
- 点击确认后新增到数据库，并刷新表格

#### 3.4.2 编辑工具
- 点击工具对应行的「编辑」按钮，弹出编辑弹窗
- 弹窗预填当前数据
- 可修改字段：工具名称、工具作用、工具地址、备注信息
- 点击确认后保存更新

#### 3.4.3 删除工具
- 点击工具对应行的「删除」按钮，弹出二次确认弹窗
- 确认弹窗内容：`确定要删除「{工具名称}」吗？此操作不可恢复。`
- 提供「取消」和「确认」两个按钮
  - 点击「取消」：关闭弹窗，不执行删除
  - 点击「确认」：执行删除操作，从数据库中移除该记录，刷新表格

#### 3.4.4 拖动排序
- 表格左侧第一列为ID列，同时也是**拖动手柄**
- 支持鼠标拖拽行进行上下排序
- 排序完成后自动更新数据库中的 `sort_order` 字段
- 排序支持**实时持久化**（拖拽结束后立即保存排序结果）

---

### 3.5 数据模型

**表名：** `tools`

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | INTEGER | PRIMARY KEY AUTOINCREMENT | 主键ID |
| name | TEXT | NOT NULL | 工具名称 |
| purpose | TEXT | NOT NULL | 工具作用/描述 |
| url | TEXT | NOT NULL | 工具访问地址 |
| remark | TEXT | - | 备注信息 |
| sort_order | INTEGER | DEFAULT 0 | 排序序号（数值越小越靠前） |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updated_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | 更新时间 |

---

## 4. 页面底部信息

页面底部固定显示作者信息：

```
使用问题请联系 wangkaikai01
```

---

## 5. 接口设计（API）

建议采用 RESTful 风格接口：

| 方法 | 路径 | 功能 | 权限 |
|------|------|------|------|
| POST | `/api/login` | 登录验证 | 公开 |
| POST | `/api/logout` | 退出登录 | 需登录 |
| GET | `/api/tools` | 获取工具列表 | 公开 |
| POST | `/api/tools` | 新增工具 | 需登录 |
| PUT | `/api/tools/{id}` | 编辑工具 | 需登录 |
| DELETE | `/api/tools/{id}` | 删除工具 | 需登录 |
| PUT | `/api/tools/sort` | 更新排序（批量） | 需登录 |
| GET | `/api/check-auth` | 检查当前登录状态 | - |

---

## 6. 非功能需求

| 类别 | 需求说明 |
|------|----------|
| 安全性 | 密码以哈希形式存储（如 bcrypt），不得明文存储 |
| 会话管理 | 使用 Cookie/Session 或 JWT Token 保持登录状态 |
| 端口配置 | 服务默认监听 `8080` 端口（可配置） |
| 数据库文件 | 默认在程序同级目录生成 `tools.db` 文件 |
| 启动初始化 | 首次启动时自动创建数据库表和表结构 |
| 静态资源 | 使用 Go `embed` 将 HTML/CSS/JS 打包进二进制文件，无需外部依赖 |
| 兼容性 | 前端需兼容主流现代浏览器（Chrome、Firefox、Edge、Safari 最新两个版本） |

---

## 7. 边界场景与异常处理

| 场景 | 处理方式 |
|------|----------|
| 访问不存在的工具ID | 返回 404，提示「工具不存在」 |
| 新增/编辑时工具名称为空 | 前端校验阻止提交，后端同样做校验 |
| URL格式不合法 | 前端校验 + 后端校验，提示「请输入有效的URL地址」 |
| 未登录访问管理接口 | 返回 401，提示「请先登录」 |
| 会话过期 | 前端检测到 401 后自动跳转至登录状态，提示「会话已过期，请重新登录」 |
| 数据库操作失败 | 返回 500，提示「系统异常，请稍后重试」 |
| 新增工具URL重复 | 允许重复URL（不视为冲突），不做唯一性校验 |
| 删除时工具被其他关联引用 | 暂无关联引用，直接删除 |

---

## 8. UI/UX 设计要求

| 要求 | 说明 |
|------|------|
| 风格 | 简洁、清晰、偏后台管理风格 |
| 表格 | 条纹状表格，鼠标悬停高亮 |
| 响应式 | 支持常见屏幕尺寸（1280px以上最佳，最小支持1024px） |
| 交互反馈 | 操作（新增/编辑/删除/排序）完成后给予明确反馈（成功/失败提示） |
| 登录弹窗 | 居中模态框，输入框带标签，密码输入框支持切换显示/隐藏 |

---

## 9. 交付物

1. **源代码**：完整 Go 项目源码
2. **二进制文件**：编译后的可执行文件（Linux/macOS/Windows 三平台）
3. **启动说明**：README 文件，包含启动命令和默认访问地址
4. **数据库初始化**：程序首次启动自动建表，无需手动执行 SQL

---

## 10. 参考资料

无

---

> **文档版本：** v1.0  
> **编写日期：** 2026-08-19  
> **目标执行对象：** Claude Code